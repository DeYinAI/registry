---
name: conventional-commit
description: Draft a conventional commit message from current changes.
---
Inspect `git status` and `git diff` (staged and unstaged). Write a conventional commit message for the changes. Use format `<type>(<scope>): <summary>` with an optional body explaining why. $ARGUMENTS
