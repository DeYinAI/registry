{
  "mcpServers": {
    "local-vision": {
      "command": "node",
      "args": ["${pluginDir}/mcp/server.mjs"],
      "env": {
        "OLLAMA_BASE_URL": "${OLLAMA_BASE_URL}",
        "OLLAMA_VISION_MODEL": "${OLLAMA_VISION_MODEL}"
      }
    }
  }
}
